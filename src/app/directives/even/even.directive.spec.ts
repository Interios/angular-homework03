import { EvenDirective } from './even.directive';

describe('EvenDirective', () => {
  it('should create an instance', () => {
    const directive = new EvenDirective();
    expect(directive).toBeTruthy();
  });
});
